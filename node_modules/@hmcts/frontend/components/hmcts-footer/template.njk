<footer class="hmcts-footer {{- ' ' + params.classes if params.classes}}" role="contentinfo">

  <div class="hmcts-footer__container {{- ' ' + params.containerClasses if params.containerClasses}}">

    {% if params.help %}
    <h2 class="hmcts-footer__heading">{{params.help.heading}}</h2>
    <ul class="hmcts-footer__list">
      {% if params.help.email %}<li class="hmcts-footer__list-item">Email: <a class="hmcts-footer__link" href="mailto:{{ params.help.email.address }}">{{ params.help.email.html | safe if params.help.email.html else params.help.email.text }}</a></li>{% endif %}
      {% if params.help.phone %}<li class="hmcts-footer__list-item">Phone: {{ params.help.phone.html | safe if params.help.phone.html else params.help.phone.text }}</li>{% endif %}
      {% if params.help.opening %}<li class="hmcts-footer__list-item"><span class="visually-hidden">Opening times: </span>{{ params.help.opening.html | safe if params.help.opening.html else params.help.opening.text }}</li>{% endif %}
    </ul>
    {% endif %}

    <hr class="govuk-section-break govuk-section-break--l govuk-section-break--visible">

    {% if params.navigation %}
    <ul class="hmcts-footer__list hmcts-footer__list--inline">
     
      {% for item in params.navigation.items %}
      <li class="hmcts-footer__list-item">
        <a class="hmcts-footer__link" href="{{ item.href }}">{{ item.html | safe if item.html else item.text }}</a>
      </li>
      {% endfor %}

    </ul>
    {% endif %}

  </div>

</footer>